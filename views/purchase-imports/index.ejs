<div class="flex justify-between items-center mb-4">
  <h1 class="text-2xl font-bold">White Label</h1>
  <% if (user.role === 'admin') { %>
    <a href="/purchase-imports/create" class="bg-blue-600 text-white px-4 py-2 rounded text-sm hover:bg-blue-700">+ Input Pembelian</a>
  <% } %>
</div>
<div class="bg-white shadow rounded overflow-x-auto">
  <table class="w-full text-sm">
    <thead><tr class="bg-gray-100">
      <th class="p-3 text-left">Produk</th>
      <th class="p-3 text-left">Vendor</th>
      <%# Kolom Negara (tiket 09). Em dash untuk pembelian tanpa negara —
          bukan string "null" atau "undefined", yang akan terbaca sebagai
          data nyata oleh siapa pun yang melihatnya. %>
      <th class="p-3 text-left">Negara</th>
      <th class="p-3 text-left">Tanggal</th>
      <th class="p-3 text-right">Qty</th>
      <th class="p-3 text-right">HPP/Item</th>
      <th class="p-3 text-left">Status</th>
      <th class="p-3 text-left">Aksi</th>
    </tr></thead>
    <tbody>
      <% imports.forEach(i => { %>
      <tr class="border-t hover:bg-gray-50">
        <td class="p-3"><%= i.nama_produk %> (<%= i.warna %> - <%= i.size %>)</td>
        <td class="p-3"><%= i.vendor_nama %></td>
        <td class="p-3"><%= i.negara_nama || '—' %></td>
        <td class="p-3"><%= i.tgl_beli %></td>
        <td class="p-3 text-right"><%= i.qty %></td>
        <td class="p-3 text-right">Rp <%= Number(i.hpp_per_item).toLocaleString('id-ID') %></td>
        <td class="p-3">
          <span class="px-2 py-0.5 text-xs rounded-full
            <%= i.status === 'pending' ? 'bg-yellow-100 text-yellow-700' : '' %>
            <%= i.status === 'validated' ? 'bg-green-100 text-green-700' : '' %>
            <%= i.status === 'rejected' ? 'bg-red-100 text-red-700' : '' %>">
            <%= i.status === 'pending' ? 'Pending' : i.status === 'validated' ? 'Valid' : 'Ditolak' %>
          </span>
        </td>
        <td class="p-3">
          <a href="/purchase-imports/<%= i.id %>" class="text-blue-600 hover:underline">Detail</a>
          <%# Edit hanya untuk pembelian pending, dan hanya admin. Yang sudah
              divalidasi sudah masuk HPP dan stok (penjaga yang sama dengan
              tiket 07 pada purchase order). %>
          <% if (i.status === 'pending' && user.role === 'admin') { %>
            <a href="/purchase-imports/<%= i.id %>/edit" class="ml-2 text-blue-600 hover:underline">Ubah</a>
          <% } %>
        </td>
      </tr>
      <% }) %>
      <% if (imports.length === 0) { %><tr><td colspan="8" class="p-3 text-center text-gray-400">Belum ada pembelian</td></tr><% } %>
    </tbody>
  </table>
</div>
